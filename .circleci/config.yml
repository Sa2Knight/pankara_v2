version: 2.1
jobs:
  build:
    docker:
      - image: ruby:2.5.1
    working_directory: /usr/local/pankara
    steps:
      - checkout
      - run:
          name: システムパッケージのインストール
          command: |
            apt-get update -qq
            apt-get install -y mysql-client build-essential
      - run:
          name: bundle install
          command: bundle install
